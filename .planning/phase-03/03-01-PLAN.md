---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SurfvidApp/AppViewModel.swift
  - SurfvidApp/ContentView.swift
  - SurfvidApp/Skim/SkimView.swift
  - SurfvidApp/Review/ReviewView.swift
autonomous: true
requirements:
  - REV-03
user_setup: []

must_haves:
  truths:
    - "Tapping Done in SkimView transitions the app to a Review screen rendered in landscape"
    - "Tapping Back in the Review screen returns to SkimView with the same video and same playhead position"
    - "PlayerController is not re-initialized when transitioning Skim → Review → Skim (clips and currentTime survive)"
    - "App builds with the new Screen enum case and ZStack branch"
  artifacts:
    - path: "SurfvidApp/AppViewModel.swift"
      provides: "Screen enum with .review case"
      contains: "case library, skim, review"
    - path: "SurfvidApp/ContentView.swift"
      provides: ".review branch in ZStack switch and .onChange orientation lock"
      contains: "case .review"
    - path: "SurfvidApp/Skim/SkimView.swift"
      provides: "Done button wired to appViewModel.screen = .review"
      contains: "appViewModel.screen = .review"
    - path: "SurfvidApp/Review/ReviewView.swift"
      provides: "Minimal landscape dark-theme view with top chrome and Back button"
      min_lines: 40
  key_links:
    - from: "SurfvidApp/Skim/SkimView.swift"
      to: "AppViewModel.screen"
      via: "Done button action assigns .review"
      pattern: "appViewModel\\.screen = \\.review"
    - from: "SurfvidApp/Review/ReviewView.swift"
      to: "AppViewModel.screen"
      via: "Back button action assigns .skim"
      pattern: "appViewModel\\.screen = \\.skim"
    - from: "SurfvidApp/ContentView.swift"
      to: "AppDelegate.lockOrientation"
      via: ".onChange handler for .review case"
      pattern: "case \\.review:[^}]*lockOrientation\\(\\.landscape\\)"
---

<objective>
Deliver the thinnest end-to-end Review routing slice: user taps Done in SkimView and lands on a real (but minimally populated) ReviewView locked to landscape; tapping Back returns to SkimView with playback state intact. This plan establishes the screen plumbing required by every Phase 3 requirement and proves REV-03 (return path) end-to-end.

Purpose: Without routing wired and the new screen registered in the ZStack and orientation lock handler, no clip list or delete behavior can be demonstrated. REV-03's "return to skimming without losing playback state" is satisfied entirely by this plan because PlayerController already survives screen swaps (Phase 1 D-10) — this plan only has to avoid breaking that invariant by adding the route.

Output:
- Extended Screen enum with .review case (D-08)
- ZStack and orientation-lock switch branches for .review (D-09)
- SkimView Done button wired to .review (D-10)
- New SurfvidApp/Review/ReviewView.swift containing dark-theme scaffold and Back-to-Skim chrome (D-01, D-05, D-10)
</objective>

## Phase Goal

**As a** Surfvid user finishing a skim session, **I want to** tap Done and land on a dedicated Review screen — and tap Back to return to the same video at the same playhead — **so that** I can move between skimming and reviewing without losing playback state.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase-03/03-CONTEXT.md
@.planning/phase-03/03-RESEARCH.md
@.planning/phase-03/03-PATTERNS.md
@.planning/phase-01/01-UI-SPEC.md
@SurfvidApp/AppViewModel.swift
@SurfvidApp/ContentView.swift
@SurfvidApp/Skim/SkimView.swift

<interfaces>
<!-- Existing contracts the executor must use directly. Do not redefine. -->

From SurfvidApp/AppViewModel.swift (existing — top-level enum, not nested):
- `enum Screen { case library, skim }` at line 5 — top-level (outside the class). Add `review` as the third case.
- `class AppViewModel: ObservableObject` with `@Published var screen: Screen = .library` at line 8.
- `@Published var clips: [Clip] = []` at line 19 — survives screen transitions (no reset on .review).
- `let playerController: PlayerController` at line 22 — created once in init, never re-created. Skim → Review → Skim preserves currentTime automatically because PlayerController is a stored property on AppViewModel, which is a @StateObject in SurfvidApp.swift.

From SurfvidApp/ContentView.swift (existing):
- `ZStack { switch appViewModel.screen { case .library: LibraryView()... case .skim: SkimView()... } }` lines 7–15
- `.onChange(of: appViewModel.screen) { newScreen in switch newScreen { case .library: AppDelegate.lockOrientation(.portrait) case .skim: AppDelegate.lockOrientation(.landscape) } }` lines 18–25
- Animation: `.animation(.easeOut(duration: 0.2), value: appViewModel.screen)` line 17 — applies opacity transition to every screen swap automatically; do not add per-view animations.

From SurfvidApp/Skim/SkimView.swift (existing):
- Line 141: `Button("Done") { /* Phase 3: trigger review screen */ }` — the stub to replace.
- Top chrome pattern (lines 116–158): HStack with Back button (chevron.left + label, regular weight 16pt), Spacer, title ("Video"), Spacer, Done pill. Wrapped in LinearGradient `[Color.black.opacity(0.45), .clear]` top→bottom. Padded `.horizontal, 18` and `.top, 14`.
- Landscape insets (lines 102–103): `.padding(.leading, 60)` for Dynamic Island, `.padding(.trailing, max(geometry.safeAreaInsets.trailing, 34))` for home indicator.

From SurfvidApp/AppDelegate.swift (existing):
- `AppDelegate.lockOrientation(_ orientation: UIInterfaceOrientationMask)` — static method already in use; idempotent.

From SurfvidApp/Shared/Formatters.swift (existing, top-level, no import needed):
- `func formatTimecode(_ seconds: Double) -> String` — produces `M:SS.t` (or `H:MM:SS.t` over an hour). Used by Plan 02; not consumed in Plan 01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Screen enum and ContentView routing for .review</name>
  <files>SurfvidApp/AppViewModel.swift, SurfvidApp/ContentView.swift</files>
  <read_first>
    - SurfvidApp/AppViewModel.swift (full file — confirm Screen enum is top-level at line 5, NOT nested in AppViewModel; do not change any other line)
    - SurfvidApp/ContentView.swift (full file — confirm current switch shape on lines 8–15 and 18–25)
    - .planning/phase-03/03-CONTEXT.md (decisions D-08, D-09)
    - .planning/phase-03/03-RESEARCH.md (Pattern 3 "Screen Enum Extension and ContentView Routing")
  </read_first>
  <action>
    In SurfvidApp/AppViewModel.swift, edit line 5 to add the third enum case so the declaration reads `enum Screen { case library, skim, review }`. Do not move the enum inside the class; it must stay top-level. Do not add any new @Published property, do not change the existing clips array, do not introduce any reset logic — D-04 (committed checkpoint) and D-06 (resume same playhead) are guaranteed by leaving clips and PlayerController untouched on the .review transition.

    In SurfvidApp/ContentView.swift, extend the ZStack switch (currently two cases at lines 8–15) to add a third branch `case .review: ReviewView().transition(.opacity)` placed after the `.skim` branch. Extend the `.onChange(of: appViewModel.screen)` switch (currently two cases at lines 18–25) to add `case .review: AppDelegate.lockOrientation(.landscape)` after the `.skim` branch, per D-09 (Review locks landscape identical to Skim). Both switches remain exhaustive; do not introduce `@unknown default`. Keep the existing `.animation(.easeOut(duration: 0.2), value: appViewModel.screen)` modifier on line 17 unchanged — it already covers the new transition.
  </action>
  <verify>
    <automated>xcodebuild build -project /Users/alexanderlaprell/repos/surfvid/Surfvid.xcodeproj -scheme Surfvid -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "case library, skim, review" SurfvidApp/AppViewModel.swift` returns line 5
    - `grep -c "case \.review:" SurfvidApp/ContentView.swift` returns 2 (one in ZStack switch, one in onChange switch)
    - `grep -n "ReviewView()" SurfvidApp/ContentView.swift` returns a line inside the ZStack switch with `.transition(.opacity)` on the same statement
    - `grep -n "AppDelegate.lockOrientation(.landscape)" SurfvidApp/ContentView.swift` returns at least 2 lines (one for .skim, one for .review)
    - xcodebuild reports BUILD FAILED with an error referencing `cannot find 'ReviewView' in scope` (expected — ReviewView is created in Task 2 of this plan). Once Task 2 lands, the build must succeed (final acceptance is on the plan's overall verify).
  </acceptance_criteria>
  <done>Screen enum has three cases; ContentView routes and orientation-locks .review identically to .skim; remaining build error is the expected missing ReviewView type, resolved by Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ReviewView skeleton with dark-theme chrome and Back button</name>
  <files>SurfvidApp/Review/ReviewView.swift</files>
  <read_first>
    - SurfvidApp/Skim/SkimView.swift (lines 24–110 ZStack scaffold + lines 116–158 topChrome — exact pattern to mirror)
    - .planning/phase-01/01-UI-SPEC.md (Skim screen dark theme section: Color.black background, Color.white text, gradient Color.black.opacity(0.45) → clear, SF Symbol chevron.left regular)
    - .planning/phase-03/03-CONTEXT.md (decisions D-01, D-05, D-10)
    - .planning/phase-03/03-PATTERNS.md (ReviewView pattern section — clipRow and topChrome substitutions)
    - .planning/phase-03/03-RESEARCH.md (Pattern 5 "Top Chrome on ReviewView")
    - SurfvidApp/AppViewModel.swift (confirm @Published clips array exists — referenced in this plan as a "no clips yet" empty placeholder, full rendering happens in Plan 02)
  </read_first>
  <action>
    Create the new file SurfvidApp/Review/ReviewView.swift (XcodeGen sources path is SurfvidApp/ and scans subdirectories recursively per project.yml — no project.yml edit needed). Import SwiftUI only (no AVFoundation; ReviewView does not touch the player).

    Declare `struct ReviewView: View` with a single `@EnvironmentObject var appViewModel: AppViewModel` property — mirror the property style used at SkimView.swift line 5. No @State, no local clip cache; ReviewView reads only from appViewModel.

    Body uses the same scaffold pattern as SkimView body: `GeometryReader { geometry in ZStack(alignment: .top) { ... } }` with `.background(Color.black).ignoresSafeArea()` modifiers at the GeometryReader outer level — copy the structure from SkimView lines 24–110, but drop all player, gesture, HUD, pending-pill, saved-pill, and bottom chrome layers. Required layers: (1) `Color.black.ignoresSafeArea()` backdrop, (2) a content layer that, for this plan, is just a placeholder `Color.clear` filling the available space (Plan 02 will replace this with the List), (3) top chrome overlay pinned by a `VStack(spacing: 0) { topChrome; Spacer() }` with `.padding(.leading, 60)` and `.padding(.trailing, max(geometry.safeAreaInsets.trailing, 34))` exactly as SkimView lines 102–103.

    Implement `private var topChrome: some View` mirroring SkimView topChrome (lines 116–158) with these exact substitutions per D-05 and D-10: (a) Back button action `{ appViewModel.screen = .skim }` (NOT `.library`); (b) Back button label text "Skim" with chevron.left SF Symbol regular weight 16pt and `.foregroundColor(.white)`; (c) `.accessibilityLabel("Back to Skim")`; (d) center title `Text("Review").font(.body.weight(.medium)).foregroundColor(Color.white.opacity(0.9))`; (e) replace the SkimView Done pill with `Color.clear.frame(width: 60, height: 1)` to balance the HStack width — there is NO Done button or Export button in ReviewView (D-04 committed checkpoint; export is Phase 4). Apply `.padding(.horizontal, 18)`, `.padding(.top, 14)`, and the gradient `LinearGradient(colors: [Color.black.opacity(0.45), .clear], startPoint: .top, endPoint: .bottom)` as background — identical to SkimView lines 149–157.

    Do NOT add a clip list, ForEach, swipe gesture, empty-state view, or any reference to appViewModel.clips in this task — Plan 02 owns those. Do NOT add a preview provider (no other view in the project uses one).
  </action>
  <verify>
    <automated>xcodebuild build -project /Users/alexanderlaprell/repos/surfvid/Surfvid.xcodeproj -scheme Surfvid -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f SurfvidApp/Review/ReviewView.swift` exits 0
    - `grep -c "^import SwiftUI" SurfvidApp/Review/ReviewView.swift` returns 1
    - `grep -c "^import AVFoundation" SurfvidApp/Review/ReviewView.swift` returns 0 (AVFoundation not imported)
    - `grep -n "struct ReviewView: View" SurfvidApp/Review/ReviewView.swift` returns one line
    - `grep -n "@EnvironmentObject var appViewModel: AppViewModel" SurfvidApp/Review/ReviewView.swift` returns one line
    - `grep -n "appViewModel.screen = .skim" SurfvidApp/Review/ReviewView.swift` returns at least one line (Back button action)
    - `grep -n 'accessibilityLabel("Back to Skim")' SurfvidApp/Review/ReviewView.swift` returns one line
    - `grep -n 'Text("Review")' SurfvidApp/Review/ReviewView.swift` returns one line
    - `grep -c "appViewModel.screen = .review" SurfvidApp/Review/ReviewView.swift` returns 0 (no self-navigation)
    - `grep -c "appViewModel.clips" SurfvidApp/Review/ReviewView.swift` returns 0 (no clip rendering in this plan)
    - `wc -l SurfvidApp/Review/ReviewView.swift` reports at least 40 lines
    - xcodebuild exits 0 (BUILD SUCCEEDED) — combined with Task 1 changes, the project now compiles
  </acceptance_criteria>
  <done>ReviewView.swift compiles into the target; the file exposes a dark-theme landscape view with a Back-to-Skim chrome button and an empty content area awaiting Plan 02's clip list.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire SkimView Done button to .review</name>
  <files>SurfvidApp/Skim/SkimView.swift</files>
  <read_first>
    - SurfvidApp/Skim/SkimView.swift (line 141 — the existing Done button stub)
    - .planning/phase-03/03-CONTEXT.md (D-10)
    - .planning/phase-03/03-RESEARCH.md (Pitfall 3 "Done Button Stub Not Wired" and Open Question 2 recommendation)
  </read_first>
  <action>
    In SurfvidApp/Skim/SkimView.swift line 141, replace the Button("Done") stub body. The new line MUST be `Button("Done") { appViewModel.screen = .review }` per D-10. Do not touch the chained modifiers on lines 142–147 (.font, .padding, .background, .foregroundColor, .clipShape) — they remain identical so the pill's visual style is unchanged.

    Add `.disabled(appViewModel.clips.isEmpty)` as a new modifier chained after `.clipShape(Capsule())` on line 147, before the closing of the topChrome HStack. This implements the RESEARCH.md Open Question 2 recommendation: prevent navigation to an empty Review when no clips are marked. (The empty-state UI inside ReviewView is added by Plan 02 as a defensive fallback for the in-Review delete-all case.)

    Do not change the placeholder title "Video" or the back-to-Library button on lines 132–136 — those are Phase 4/title-binding scope.
  </action>
  <verify>
    <automated>xcodebuild build -project /Users/alexanderlaprell/repos/surfvid/Surfvid.xcodeproj -scheme Surfvid -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Button("Done") { appViewModel.screen = .review }' SurfvidApp/Skim/SkimView.swift` returns line 141 (or the same logical statement; allow whitespace variants)
    - `grep -c "Phase 3: trigger review screen" SurfvidApp/Skim/SkimView.swift` returns 0 (stub comment removed)
    - `grep -n ".disabled(appViewModel.clips.isEmpty)" SurfvidApp/Skim/SkimView.swift` returns one line inside the topChrome view
    - xcodebuild exits 0 (BUILD SUCCEEDED)
  </acceptance_criteria>
  <done>Tapping Done in SkimView assigns appViewModel.screen = .review when at least one clip exists; tapping Done with zero clips is disabled (button non-interactive). Build is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Device/simulator verification of routing slice</name>
  <what-built>SkimView Done button now navigates to a new ReviewView (dark-theme, landscape, with Back button); Back returns to SkimView with playback state intact. No clip list yet — that arrives in Plan 02.</what-built>
  <how-to-verify>
    1. Build and run on simulator: `xcodebuild -project Surfvid.xcodeproj -scheme Surfvid -destination 'platform=iOS Simulator,name=iPhone 16' build` then launch from Xcode or `xcrun simctl launch booted com.laprell.surfvid`.
    2. Grant Photos access (if first launch) and tap any video → SkimView opens in landscape.
    3. Mark at least one clip (tap IN, scrub, tap OUT) so the Done button becomes enabled.
    4. Confirm the Done button visually shows as enabled (full white pill) — the .disabled modifier removes the tint when clips are present.
    5. Tap Done. Expect: instant opacity transition to a black landscape screen with "← Skim   Review   [empty space]" top chrome. No crash, no flash of portrait, no white List background.
    6. Tap the "Skim" Back button in the top-left. Expect: returns to SkimView. The same video plays. The timecode displayed matches (within ~0.1s) the position where Done was tapped. The clips marked before Done are still listed at the bottom ("N marked" indicator unchanged).
    7. Tap Done again → land in Review. Tap Skim. Repeat once more. Confirm no clip duplication, no state loss, no orientation glitch (screen stays landscape throughout the Skim ↔ Review cycle).
    8. Tap the "Library" Back button in SkimView → returns to portrait Library. Reopen the same video and confirm Phase 1/2 behaviors are unaffected.
    9. Edge case: after entering a new video, do NOT mark any clips. Verify the Done pill is rendered in its disabled state and tapping it does nothing (still on SkimView).
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 steps pass. Otherwise describe the failure (screen, expected, actual) and the planner will revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User gesture → AppViewModel state | Tap on Done / Back buttons mutates `screen` enum directly. No external input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Screen enum mutation from background thread | accept | All button actions run on MainActor by virtue of SwiftUI button handlers. No async/Task is introduced in this plan. |
| T-03-02 | Denial of Service | Done tapped repeatedly with empty clip list | mitigate | `.disabled(appViewModel.clips.isEmpty)` on Done button (Task 3) prevents navigation to an empty Review and reduces orientation-lock thrash. |
| T-03-03 | Information Disclosure | None | accept | No PII, no network, no persistence layer touched in Phase 3. |
</threat_model>

<verification>
- Build succeeds after all three tasks (`xcodebuild build -project Surfvid.xcodeproj -scheme Surfvid -destination 'platform=iOS Simulator,name=iPhone 16'` exits 0)
- Screen enum has exactly three cases (library, skim, review)
- ContentView ZStack and onChange handle .review explicitly (no `@unknown default`)
- ReviewView.swift exists at SurfvidApp/Review/ReviewView.swift, imports SwiftUI only, exports `struct ReviewView: View`
- SkimView Done button no longer contains the Phase 3 stub comment
- Manual checkpoint (Task 4) confirms Skim ↔ Review round-trip preserves playback time and clips
</verification>

<success_criteria>
- REV-03 demonstrably satisfied: user can return from Review to Skim with same video and same playhead position
- Screen routing infrastructure exists for Plan 02 to render the clip list
- No regressions to LibraryView or SkimView behavior
- All build commands exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-screen/03-01-SUMMARY.md` documenting:
- Screen enum extension
- ContentView routing/orientation changes
- ReviewView skeleton structure
- SkimView Done wire-up + empty-clip guard
- Manual verification results
</output>
