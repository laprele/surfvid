---
phase: 03
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - SurfvidApp/Review/ReviewView.swift
autonomous: false
requirements:
  - REV-01
  - REV-02
user_setup: []

must_haves:
  truths:
    - "User sees one row per clip in appViewModel.clips with start time, end time, and duration formatted via formatTimecode"
    - "Swiping a row from right-to-left reveals a destructive Delete action; full-swipe or tapping Delete removes that clip immediately with no confirmation"
    - "After deletion, appViewModel.clips no longer contains the deleted clip and the row disappears from the list"
    - "When appViewModel.clips is empty (e.g. user deleted all clips while in Review), ReviewView shows an empty-state message instead of a blank scroll surface"
    - "List renders with full dark theme: no white/gray cell backgrounds, no system List background visible"
    - "Returning from Review to Skim preserves the (possibly modified) clips array — deletions made in Review persist"
  artifacts:
    - path: "SurfvidApp/Review/ReviewView.swift"
      provides: "Clip list rendering, swipe-to-delete, dark-theme List styling, empty state"
      contains: ".swipeActions(edge: .trailing"
      min_lines: 80
  key_links:
    - from: "SurfvidApp/Review/ReviewView.swift"
      to: "AppViewModel.clips"
      via: "ForEach(appViewModel.clips) iterates by Identifiable id (UUID)"
      pattern: "ForEach\\(appViewModel\\.clips\\)"
    - from: "SurfvidApp/Review/ReviewView.swift"
      to: "AppViewModel.clips.remove"
      via: "Delete button action uses firstIndex(where: { $0.id == clip.id })"
      pattern: "firstIndex\\(where: \\{ \\$0\\.id == clip\\.id"
    - from: "SurfvidApp/Review/ReviewView.swift"
      to: "Formatters.formatTimecode"
      via: "Each row formats clip.start, clip.end, and clip.end - clip.start"
      pattern: "formatTimecode\\(clip\\."
---

<objective>
Replace the empty placeholder in ReviewView (created in Plan 01) with the real clip list: text-only rows showing `[start → end] (duration)` per D-02, swipe-to-delete via `.swipeActions(edge: .trailing)` with `Button(role: .destructive)` per D-03, full dark-theme List styling, and a defensive empty-state view for the case where the user deletes every clip while inside Review.

Purpose: This is the slice that delivers REV-01 (clip list visibility) and REV-02 (delete behavior). The routing scaffolding from Plan 01 is already in place; this plan touches only ReviewView.swift to upgrade its content layer.

Output:
- ReviewView clip rows with start/end timecodes and duration (D-02)
- Swipe-to-delete with immediate removal, no confirmation (D-03)
- Dark theme: `.scrollContentBackground(.hidden)` + `.listRowBackground(Color.clear)` (Pitfall 1 mitigation)
- Stable Identifiable-keyed ForEach (Pitfall 6 mitigation)
- Index-by-id deletion pattern (Pitfall 2 mitigation)
- Empty-state defensive UI (Pitfall 5 mitigation)
</objective>

## Phase Goal

**As a** Surfvid user reviewing a session, **I want to** see every clip I marked with its start, end, and duration, swipe to delete any I do not want, **so that** I can curate my list before exporting.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phase-03/03-CONTEXT.md
@.planning/phase-03/03-RESEARCH.md
@.planning/phase-03/03-PATTERNS.md
@.planning/phase-01/01-UI-SPEC.md
@.planning/phases/03-review-screen/03-01-SUMMARY.md
@SurfvidApp/Review/ReviewView.swift
@SurfvidApp/AppViewModel.swift
@SurfvidApp/Shared/Formatters.swift

<interfaces>
<!-- Existing contracts the executor must use directly. -->

From SurfvidApp/AppViewModel.swift (existing):
- `struct Clip: Identifiable { let id = UUID(); let start: Double; let end: Double }` lines 13–17 — already Identifiable; ForEach can iterate `appViewModel.clips` without `id:` parameter.
- `@Published var clips: [Clip] = []` line 19 — direct mutation `clips.remove(at:)` triggers SwiftUI re-render. AppViewModel is a class with @Published, so removal must be synchronous on MainActor (no Task/Dispatch wrap).

From SurfvidApp/Shared/Formatters.swift (existing, top-level func):
- `func formatTimecode(_ seconds: Double) -> String` — produces `M:SS.t` under one hour, `H:MM:SS.t` otherwise. Available without import.

From SurfvidApp/Library/LibraryView.swift (analog for empty-state pattern):
- Empty-state idiom: VStack(spacing: 16) { Spacer; Image(systemName:) + thin weight; Text headline + Text body; Spacer }. Adapt foregrounds for dark theme (Color.white, Color.white.opacity(0.55)).

From SwiftUI (iOS 16.0+ system, deployment target matches):
- `.scrollContentBackground(_ visibility: Visibility)` — pass `.hidden` to suppress the List's system background.
- `.listRowBackground(_:)` — apply `Color.clear` to each row content for true dark theme.
- `.swipeActions(edge: .trailing, allowsFullSwipe: Bool, content:)` — row-level modifier. `allowsFullSwipe: true` (default) commits the first destructive action on full swipe — required by D-03 (immediate, no confirm).
- `Button(role: .destructive) { ... } label: { Label("Delete", systemImage: "trash") }` — system applies systemRed tint and VoiceOver "Deletes" semantic automatically.

From SurfvidApp/Review/ReviewView.swift (created in Plan 01):
- Existing structure: `GeometryReader { ZStack(alignment: .top) { Color.black.ignoresSafeArea(); <content layer placeholder>; VStack { topChrome; Spacer } } }`. Replace the `<content layer placeholder>` only — do not touch topChrome or the outer scaffold padding (60pt leading / safeAreaInsets.trailing).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace ReviewView content layer with dark-theme List + swipe-to-delete + empty state</name>
  <files>SurfvidApp/Review/ReviewView.swift</files>
  <read_first>
    - SurfvidApp/Review/ReviewView.swift (full file as left by Plan 01)
    - SurfvidApp/AppViewModel.swift (Clip struct lines 13–17; clips array line 19)
    - SurfvidApp/Shared/Formatters.swift (formatTimecode signature)
    - .planning/phase-03/03-CONTEXT.md (D-02, D-03, D-07)
    - .planning/phase-03/03-RESEARCH.md (Pattern 1 swipeActions, Pattern 2 dark-theme List, Pattern 4 clip row, Pitfall 1, Pitfall 2, Pitfall 5, Pitfall 6, Code Examples "Full ReviewView Skeleton")
    - .planning/phase-03/03-PATTERNS.md (clip row pattern and empty state pattern)
    - .planning/phase-01/01-UI-SPEC.md (Skim dark theme colors: Color.white primary text, Color.white.opacity(0.55) secondary, Color.white.opacity(0.12) separator)
    - SurfvidApp/Library/LibraryView.swift (empty-state structural analog — adapt colors for dark theme)
  </read_first>
  <action>
    Edit SurfvidApp/Review/ReviewView.swift. Keep the file's existing top-level scaffold from Plan 01 — `GeometryReader { ZStack(alignment: .top) { Color.black.ignoresSafeArea(); ... ; VStack { topChrome; Spacer } } }` plus `.background(Color.black).ignoresSafeArea()` and the .padding(.leading, 60) / .padding(.trailing, max(geometry.safeAreaInsets.trailing, 34)) on the VStack — unchanged. Topchrome is unchanged from Plan 01.

    Replace the empty `Color.clear` placeholder in the content layer with a conditional view that branches on `appViewModel.clips.isEmpty`. Use Swift's @ViewBuilder via `if appViewModel.clips.isEmpty { emptyState } else { clipList }` — both expressions are private computed properties added in this task.

    Implement `private var clipList: some View` as a `List { ForEach(appViewModel.clips) { clip in clipRow(clip) ... } }` chain. ForEach iterates `appViewModel.clips` directly (Clip is Identifiable via UUID per AppViewModel.swift lines 13–17) — do NOT use `id: \.self` or `Array(...enumerated())` to avoid Pitfall 6. Apply per-row modifiers `.listRowBackground(Color.clear)` and `.listRowSeparatorTint(Color.white.opacity(0.12))` to each row inside ForEach. Apply List-level modifiers `.scrollContentBackground(.hidden)` and `.listStyle(.plain)` and `.padding(.top, 56)` (clears the top chrome height — same offset SkimView uses implicitly via the gradient overlay). Both `.scrollContentBackground(.hidden)` AND `.listRowBackground(Color.clear)` are mandatory per Pitfall 1; do not omit either.

    Implement `private func clipRow(_ clip: AppViewModel.Clip) -> some View` returning an HStack containing a leading VStack(alignment: .leading, spacing: 4) with two Text views: first text is `"\(formatTimecode(clip.start)) → \(formatTimecode(clip.end))"` with `.font(.body.monospacedDigit())` (monospacedDigit prevents row jitter as digits change — established pattern from SkimView line 170) and `.foregroundColor(.white)`; second text is `formatTimecode(clip.end - clip.start)` with `.font(.caption)` and `.foregroundColor(Color.white.opacity(0.55))`. Append a trailing Spacer to the HStack. Apply `.padding(.vertical, 8)` and `.contentShape(Rectangle())` to the HStack. Do not add a tap gesture per D-07 (rows are read-only).

    Chain `.swipeActions(edge: .trailing, allowsFullSwipe: true) { Button(role: .destructive) { ... } label: { Label("Delete", systemImage: "trash") } }` onto the clipRow's HStack. Inside the destructive button action, look up the index using `if let index = appViewModel.clips.firstIndex(where: { $0.id == clip.id }) { appViewModel.clips.remove(at: index) }` — this avoids the stale-index pitfall from RESEARCH.md Pitfall 2. Do NOT wrap the removal in DispatchQueue, Task, or withAnimation; SwiftUI animates List row removal automatically when the Identifiable id disappears. Do NOT add a confirmation dialog (D-03 prohibits it).

    Implement `private var emptyState: some View` as a centered VStack(spacing: 16) on the dark backdrop: leading Spacer; `Image(systemName: "film").font(.system(size: 48, weight: .thin)).foregroundColor(Color.white.opacity(0.3))`; `Text("No clips marked").font(.title2.weight(.semibold)).foregroundColor(.white)`; `Text("Tap Skim to return and mark clips from this video.").font(.body).foregroundColor(Color.white.opacity(0.55)).multilineTextAlignment(.center).padding(.horizontal, 24)`; trailing Spacer. Wrap in `.frame(maxWidth: .infinity, maxHeight: .infinity)` so it fills the layer. This is the Pitfall 5 mitigation.

    Do not add an Export button, batch-delete, reorder gesture, or per-clip tap action — all are deferred per CONTEXT.md "Deferred Ideas". Do not change the topChrome or its Back button — Plan 01 owns that surface and it remains correct.
  </action>
  <verify>
    <automated>xcodebuild build -project /Users/alexanderlaprell/repos/surfvid/Surfvid.xcodeproj -scheme Surfvid -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ForEach(appViewModel.clips)" SurfvidApp/Review/ReviewView.swift` returns one line; no `id: \\.self` and no `enumerated()` on the same line
    - `grep -n ".scrollContentBackground(.hidden)" SurfvidApp/Review/ReviewView.swift` returns one line
    - `grep -c ".listRowBackground(Color.clear)" SurfvidApp/Review/ReviewView.swift` returns at least 1
    - `grep -n ".swipeActions(edge: .trailing" SurfvidApp/Review/ReviewView.swift` returns one line containing `allowsFullSwipe: true`
    - `grep -n "Button(role: .destructive)" SurfvidApp/Review/ReviewView.swift` returns one line
    - `grep -n 'Label("Delete", systemImage: "trash")' SurfvidApp/Review/ReviewView.swift` returns one line
    - `grep -n 'firstIndex(where: { \$0.id == clip.id })' SurfvidApp/Review/ReviewView.swift` returns one line
    - `grep -c "formatTimecode(clip." SurfvidApp/Review/ReviewView.swift` returns at least 3 (start, end, duration)
    - `grep -n 'appViewModel.clips.isEmpty' SurfvidApp/Review/ReviewView.swift` returns one line (empty-state guard)
    - `grep -n '"No clips marked"' SurfvidApp/Review/ReviewView.swift` returns one line (empty-state copy)
    - `grep -c "DispatchQueue" SurfvidApp/Review/ReviewView.swift` returns 0 (no async wrap on deletion)
    - `grep -c ".onTapGesture" SurfvidApp/Review/ReviewView.swift` returns 0 (D-07: rows read-only)
    - `wc -l SurfvidApp/Review/ReviewView.swift` reports at least 80 lines
    - xcodebuild exits 0 (BUILD SUCCEEDED)
  </acceptance_criteria>
  <done>ReviewView renders the full clip list with timecodes, supports swipe-to-delete with immediate removal, applies full dark theme to the List, and gracefully handles the empty state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Device/simulator verification of full Review behavior (REV-01, REV-02, REV-03 round-trip)</name>
  <what-built>ReviewView clip list with start/end/duration rows, swipe-to-delete (full-swipe and tap-Delete), dark theme (no white cell backgrounds), empty state when all clips deleted, and Plan-01 routing intact.</what-built>
  <how-to-verify>
    1. Build and run on simulator: `xcodebuild -project Surfvid.xcodeproj -scheme Surfvid -destination 'platform=iOS Simulator,name=iPhone 16' build && xcrun simctl launch booted com.laprell.surfvid` (or launch via Xcode).
    2. Open a video from the library; in SkimView mark at least 3 clips of different durations (e.g. one ~5s, one ~15s, one ~30s) using the IN/OUT buttons.
    3. Tap Done. ReviewView appears in landscape with a black background.
    4. Verify REV-01: each row shows `M:SS.t → M:SS.t` in monospaced digits on top and the duration `M:SS.t` in smaller muted text below. There are exactly as many rows as you marked. Order matches the order you marked them in. The List background is uniformly black — no white or light-gray cell fills anywhere.
    5. Swipe one row right-to-left a short distance. A red Delete button appears with a trash icon. Tap Delete. The row animates out; the remaining rows close the gap.
    6. Swipe another row right-to-left aggressively (full swipe). The clip should delete immediately without requiring a second tap — confirms `allowsFullSwipe: true` and D-03 "immediate removal".
    7. Verify REV-02: after the two deletions, the remaining row matches the clip you did NOT delete (verify by start time). No wrong-row deletions.
    8. Delete the final remaining clip. The empty-state view appears: a thin "film" SF Symbol, "No clips marked" heading, and the body text "Tap Skim to return and mark clips from this video."
    9. Tap the Skim Back button in the top chrome. Verify REV-03 + D-06: returns to SkimView in landscape, the same video, same playhead position, "0 marked" indicator in the bottom chrome. PlayerController did not reload (no playback pause/seek glitch).
    10. Mark one new clip in Skim. Tap Done. ReviewView shows ONE row (the new one) — confirms the clips array survived the round-trip and the prior deletions persisted.
    11. Edge case (Pitfall 2 stress): mark 4 clips, enter Review, then swipe-delete clips rapidly in quick succession (e.g. delete index 1, then immediately the new index 1, etc.). No crash, no wrong-row deletion, no out-of-bounds error.
    12. Edge case: tap Done with zero clips marked in SkimView. The Done button should be visually disabled (greyed-out) and tapping it does nothing — this is the Plan-01 guard. (If the user previously deleted everything and is currently inside Review, the empty-state path from step 8 covers it.)
    13. Accessibility quick check: VoiceOver enabled, navigate to a clip row — VoiceOver should announce the timecode text and "Delete" as a custom rotor action via swipeActions semantics.
  </how-to-verify>
  <resume-signal>Type "approved" if all 13 steps pass. Otherwise describe the failure (step number, expected, actual) and the planner will revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User gesture → clips array mutation | Swipe + tap on a destructive button removes an element from @Published clips on MainActor. No external input crosses any boundary; clip data is locally owned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Stale-index deletion (concurrent or fast successive deletes mutating wrong row) | mitigate | Use `firstIndex(where: { $0.id == clip.id })` instead of captured enumerated index — looks up by stable UUID at delete time (RESEARCH.md Pitfall 2). Synchronous removal on MainActor; no Task/DispatchQueue wrap. |
| T-03-05 | Tampering | Negative-duration clip rendered (end < start) | accept | Phase 2 `markOut` has `guard end > start` so the clip array cannot contain such entries — no defensive validation needed in ReviewView (RESEARCH.md Security Domain). |
| T-03-06 | Denial of Service | Empty clip list rendering a blank black screen with no user affordance | mitigate | Explicit empty-state view ("No clips marked" + return prompt) rendered when `appViewModel.clips.isEmpty` — covers the "user deleted all clips while inside Review" path. |
| T-03-07 | Information Disclosure | None | accept | No PII, no network, no persistence. |
</threat_model>

<verification>
- Build succeeds (`xcodebuild build` exits 0)
- ReviewView contains the required modifiers and patterns (verified by grep counts in Task 1 acceptance criteria)
- Manual device verification (Task 2) confirms REV-01 display, REV-02 swipe-delete (tap + full-swipe), empty-state UX, and round-trip persistence with REV-03 from Plan 01
- Dark-theme List renders without any visible system cell backgrounds
- No regressions to Plan 01 routing or topChrome behavior
</verification>

<success_criteria>
- REV-01: All clips visible with start/end/duration formatting via formatTimecode
- REV-02: Swipe-to-delete works (both tap-Delete and full-swipe), immediate removal, no confirmation
- REV-03 round-trip: returning to Skim preserves video, playhead, and the (possibly-modified) clips array
- Dark theme intact: no white/gray cells, no system List background visible
- Empty state handles the all-deleted case gracefully with copy that points the user back to Skim
- No new dependencies; no new Swift files; ReviewView.swift remains the only Review-specific file (~80–110 lines)
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-screen/03-02-SUMMARY.md` documenting:
- ReviewView content layer structure (List vs. emptyState branch)
- Clip row layout and formatTimecode usage
- swipeActions + Button(role: .destructive) + firstIndex(where:) deletion pattern
- Dark-theme modifier pair (.scrollContentBackground + .listRowBackground)
- Empty-state copy and trigger condition
- Manual verification results across all 13 steps
</output>
